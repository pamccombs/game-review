<!DOCTYPE html>
<html>
  <head>
    <title>Game Review</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <h1 align= center>Game Review</h1>

    <% if logged_in? %>
    <p>Welcome, <%=current_user.username%>! <%= link_to "Log Out", logout_path %>

      <div class= 'navbar'>

        <%= link_to "#{current_user.username}'s Profile", user_path(current_user) %> 
        <%= link_to "All Games", games_path(@games) %>
        <%= link_to "All Reviews", reviews_path(@reviews) %>
      
      </div>
    <% end %>

    <%= yield %>

  </body>
</html>